# E2E Test Credentials — NEVER commit real values
# Copy to .env.test and fill in real values for local testing
# .env.test is gitignored
TEST_ADMIN_USER=admin
TEST_ADMIN_PASSWORD=changeme
TEST_BASE_URL=http://localhost:5173
TEST_NODE_IP=127.0.0.1
